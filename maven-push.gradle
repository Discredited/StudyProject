// maven仓库的基本配置
apply plugin: 'maven'

uploadArchives {

    repositories {
        mavenDeployer {
            pom.groupId = ""
            pom.artifactId = ""
            pom.version = "0.0.1"
            repository(url:"file://localhost/Users/june/.m2/repository")
        }
    }


}